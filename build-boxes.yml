---
- hosts: localhost
  connection: local
  gather_facts: false

  vars:
    boxes:
      - centos7
      - rocky8
      - ubuntu1804
      - ubuntu2004
      - ubuntu2204
    versions: {}

  tasks:

    - include_tasks: shared/build-a-box.yml
      with_items: "{{ boxes }}"
